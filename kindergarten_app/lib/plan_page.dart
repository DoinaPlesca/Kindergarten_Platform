import 'package:flutter/cupertino.dart';

class PlanListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Lista cu planul pentru luna curentă',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
